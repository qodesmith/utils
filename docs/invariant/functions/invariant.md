[@qodestack/utils](../../README.md) / [invariant](../README.md) / invariant

# Function: invariant()

> **invariant**(`condition`, `message`): `asserts condition`

Defined in: [invariant.ts:8](https://github.com/qodesmith/utils/blob/454dfeeb7b8c144bcc13726c63d1753237871405/src/invariant.ts#L8)

Asserts that a condition is truthy, throwing an error if it's not.

## Parameters

### condition

`unknown`

The condition to check.

### message

`string`

The error message to throw if the condition is falsy.

## Returns

`asserts condition`

## Throws

Throws an error with the provided message if the condition is falsy.
